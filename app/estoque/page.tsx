"use client";
import {useEffect,useMemo,useState} from "react";import Header from "@/components/Header";import VehicleCard from "@/components/VehicleCard";import {supabase} from "@/lib/supabase";import {Vehicle} from "@/lib/types";
export default function Estoque(){const[v,setV]=useState<Vehicle[]>([]);const[q,setQ]=useState("");useEffect(()=>{supabase.from("vehicles").select("*").eq("status","available").order("created_at",{ascending:false}).then(({data})=>setV((data||[]) as Vehicle[]))},[]);const f=useMemo(()=>v.filter(x=>`${x.brand} ${x.model} ${x.version||""}`.toLowerCase().includes(q.toLowerCase())),[v,q]);return <><Header/><main className="container py-14"><h1 className="text-5xl font-black">Estoque</h1><input className="mt-8 w-full rounded-xl border bg-white px-4 py-3" placeholder="Marca, modelo ou versão" value={q} onChange={e=>setQ(e.target.value)}/><div className="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{f.map(x=><VehicleCard key={x.id} vehicle={x}/>)}</div></main></>}